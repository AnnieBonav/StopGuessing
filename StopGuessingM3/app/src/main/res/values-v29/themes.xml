<resources xmlns:tools="http://schemas.android.com/tools">

    <style name="Theme.StopGuessingM3" parent="Base.Theme.StopGuessingM3">
        <!-- Transparent system bars for edge-to-edge. -->
        <item name="android:navigationBarColor">@android:color/transparent</item>
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:windowLightStatusBar">?attr/isLightTheme</item>
    </style>

    <style name="CustomCardStyle" parent="Widget.MaterialComponents.CardView">
        <item name="cardBackgroundColor">@color/md_theme_dark_outlineVariant</item>
    </style>
</resources>